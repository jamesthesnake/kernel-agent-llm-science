# Core dependencies for Autonomous AI Research Agent
# Minimal installation for running the demo

# Core ML/AI frameworks
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0
tokenizers>=0.19.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Data handling and validation
pydantic>=2.5.0

# Utilities
rich>=13.0.0
tqdm>=4.65.0

# Optional: GPU optimization (comment out if no GPU)
# bitsandbytes>=0.41.0

# CUDA support (uncomment if using CUDA kernels)
# triton>=3.0.0

# Development
pytest>=7.0.0